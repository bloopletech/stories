<h1>Edit Collection</h1>
<% semantic_form_for :collection, :url => collection_path(@collection), :html => { :method => :put } do |f| %>
  <% f.inputs do %>
    <%= f.input :title %>
    <%= f.input :always_show_title, :as => :boolean %>
  <% end %>

  <%= f.buttons %>
<% end %>